
# kubectl port-forward svc/nginx-service 81:80
# 宿主机访问 http://localhost:81
apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: dev
spec:
  ports:
  - port: 80
    # protocol: TCP
    targetPort: 80
  selector:
    app: nginx
  type: ClusterIP # 只用集群内部可访问
  

